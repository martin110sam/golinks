import { ThemeOptions } from "@mui/material/styles";

export const themeOptions = () => {
  return palette: {
    mode: "light",
    primary: {
      main: "#2a2b2a",
    },
    secondary: {
      main: "#fbfbf2",
    },
  },
};
